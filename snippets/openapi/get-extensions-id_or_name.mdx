<CodeGroup>
```typescript Typescript/Javascript
import Kernel from '@onkernel/sdk';

const client = new Kernel({
  apiKey: 'My API Key',
});

const extension = await client.extensions.retrieve('id_or_name');

console.log(extension);

const content = await extension.blob();
console.log(content);
```


```python Python
from kernel import Kernel

client = Kernel(
    api_key="My API Key",
)
extension = client.extensions.retrieve(
    "id_or_name",
)
print(extension)
content = extension.read()
print(content)
```
</CodeGroup>
